// This component is no longer used in the wizard flow.
// Scoring rules are now FIXED defaults (not user-configurable).
// Tiebreaker order is fixed: set_difference -> point_difference.
// Kept for reference only.

export default function WizardStep3Qualifying() {
  return null;
}
