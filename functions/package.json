{
  "name": "showdown-functions",
  "private": true,
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.81.0",
    "firebase-admin": "^13.4.0",
    "firebase-functions": "^7.2.5"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "typescript": "~5.7.0",
    "vitest": "^4.1.3"
  }
}
